from setuptools import setup, find_packages

setup(
    name="fibonacci", #name of the package
    version="0.1", #not for public realise
    packages=find_packages() #it tells where the packages are
)